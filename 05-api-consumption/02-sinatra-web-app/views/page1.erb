<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Team Building - Activities</title>
    <%= stylesheet '/style.css' %>
  </head>
  <body>

  <div class="page-header">
        <h1>
          <%= @activity["name"]%>
        </h1>
      </div>

<div class="hero" style="background-image: url(<%= @activity["photo_url"]%>)">
    <span class="label label-primary"><%= @activity["category"]%></span>
      </div>

<div class="activity-card">

          <div class="activity-details">
            <div class="sub-header">
              <span class="city"><%= @activity["city"]%></span>
              <span class="price"><%= @activity["price"]%> euros</span>
            </div>
            <div class="details">
              <ul>
                <li class="participants">
                  <span class="icon">👥</span> <%= @activity["participants_count"]%> partipants
                </li>
                <li class="duration">
                  <span class="icon">⏱</span> <%= @activity["duration"]%> min
                </li>
              </ul>
              <div class="cta">
                <a href="" class="btn btn-bold btn-primary link">Book (not working)</a>
              </div>
            </div>
          </div>
        </div>
  </body>
</html>
